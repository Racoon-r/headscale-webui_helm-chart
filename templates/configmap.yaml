apiVersion: v1
kind: ConfigMap
metadata:
  name: config-headscale
  labels:
    {{- include "headscale-webui.labels" . | nindent 4 }}
data:
  config.yaml: |-
    {{ toYaml .Values.configHeadscale | quote | indent 4 }}
